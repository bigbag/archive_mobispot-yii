'use strict';

function MenuController($scope)
{
    //Авторизация
    $scope.login = function(user)
    {
        alert(1);
    };
}

function UserController($scope, $http, $compile)
{
    //Авторизация
    $scope.login = function(user)
    {
        alert(1);
    };
}
